# -*- coding: utf-8 -*-
# vim: ft=yaml
monit:
  pkg: monit
  config_file: '/etc/monit/monitrc'
  config_includes: '/etc/monit/conf.d'
  logfile: 'syslog'
  daemon_interval: 30
  service:
    name: monit
